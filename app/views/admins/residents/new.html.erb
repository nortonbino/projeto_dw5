<div class="container">
	
  <%= provide(:title, 'Sign Up Resident') %>


  <form>
    <label>Add the admin by email</label>
    <%= text_field_tag :resident_mail %>
    <%= button_to 'Add', class: "btn  cyan darken-4" %>
  </form>
  
  <% if !@resident_by_email %>
  
  <h5> This resident doesn't exist, create an account to him </h5>
  <%= simple_form_for(@resident, :url => admins_condominium_residents_path(@condominium)) do |f| %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :email, required: true %>
    <%= f.input :password, required: true %>
    <%= f.input :password_confirmation, required: true %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
  <% end %>

  <% end %>

</div>